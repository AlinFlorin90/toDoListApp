import React from "react";
import {Container,Paragraf} from "./Header.style";

const Header = () => {
    return (
    <Container>
        <Paragraf>To-Do List</Paragraf>
    </Container>);
};

export default Header;